[package]
name = "http-sink"
group = "infinyon"
version = "0.2.9"
apiVersion = "0.1.0"
fluvio = "0.11.16"
description = "HTTP sink connector reads records from data streaming and generates an HTTP request"
license = "Apache-2.0"
visibility = "public"

[direction]
dest = true

[deployment]
binary = "http-sink"
